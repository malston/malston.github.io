# ABOUTME: Development override with Hugo live reload
# ABOUTME: Run: docker compose up hugo

services:
  hugo:
    image: hugomods/hugo:exts
    command: hugo server --bind 0.0.0.0 --baseURL http://localhost:1313
    ports:
      - "1313:1313"
    volumes:
      - .:/src
    working_dir: /src
